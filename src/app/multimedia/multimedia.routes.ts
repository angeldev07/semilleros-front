import { Routes } from "@angular/router";
import { MultimediaComponent } from "./multimedia.component";

export const MULTIMEDIA_ROUTES: Routes = [
    {
        path: '',
        component: MultimediaComponent
    }
]